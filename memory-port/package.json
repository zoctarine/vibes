{
  "name": "conversation-summary-manager",
  "version": "1.0.0",
  "description": "MCP server for conversation summary management with Supabase",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && echo 'Build complete - index.js overwritten'",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "start:with-env": "node dist/index-with-env.js",
    "start:direct": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.5.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
